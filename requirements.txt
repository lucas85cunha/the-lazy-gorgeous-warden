# ============================================================================
# The-Lazy-Gorgeous-Warden — Python Dependencies
# ============================================================================
# Only direct dependencies are pinned here. Transitive dependencies are
# managed automatically by pip.  Run: pip install -r requirements.txt
# ============================================================================

# --- Core AI Engine ---
# Modern 2026 SDK for Google Gemini (replaces legacy google-generativeai)
google-genai>=1.0.0

# --- Configuration ---
# Environment variable management via .env files
python-dotenv>=1.0.1
